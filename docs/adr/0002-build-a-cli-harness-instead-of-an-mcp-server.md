# Build A CLI Harness Instead Of An MCP Server

Neowright will be a standalone CLI harness rather than an MCP server. The tool should work in scripts, terminals, and agent workflows without depending on a specific agent runtime; agents learn to use it through a bundled skill instead of through a server protocol.

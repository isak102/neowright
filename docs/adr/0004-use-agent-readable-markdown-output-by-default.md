# Use Agent-Readable Markdown Output By Default

Neowright will use structured Markdown output by default rather than JSON-first or prose-only output. This matches the agent-facing style of `playwright-cli`: command output remains readable in terminals and transcripts while exposing stable sections, code blocks, metadata, and artifact links that agents can interpret reliably.
